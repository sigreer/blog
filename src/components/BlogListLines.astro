---
import { readItems } from "@directus/sdk";
import directus from "../lib/directus";
import BlogListLine from "./BlogListLine.astro";
const posts = await directus.request(readItems("posts", {
  fields: ['*'],
}));
---

<section>
  <h2 class="mb-2 text-xl md:mb-4 md:text-2xl lg:text-3xl font-bold">Blog Post Lines</h2>
    <ul class="flex flex-col gap-y-2 md:gap-y-3 lg:gap-y-6">
        {
          posts.slice(2, 6).map((post) => (
            <BlogListLine
              title={post.title}
              slug={post.slug}
              content={post.excerpt}
              tags={post.tags}
              image={post.image}
            />
          ))
        }
      </ul>
</section>
  
<div class="my-4 md:my-6"></div>